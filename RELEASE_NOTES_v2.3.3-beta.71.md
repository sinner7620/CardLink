# v2.3.3-beta.71

## 修复与重构

- 错题本列表和详情彻底共用白色底层，清除四周页面底色与外边距，仅保留分栏竖线和控件自身留白。
- 列表操作区重做为与详情一致的圆角毛玻璃悬浮条；内部控件去除重复底色和边框，搜索框单独保留描边，批量菜单降为图标文字层。
- 筛选悬浮条移出列表滚动裁剪层，分类和日期弹层提高到详情区之上，修复下拉框被预览遮挡。
- 详情工具条降低不透明度和模糊强度，让题目文字可透出但仍保持控件辨识度。
- 窄窗口下等级与剩余天数改为等宽纵向拼接，修复两枚胶囊错位。
- 同步开关明确关闭且定位未成功时返回针对性失败提示；页面通知改为 fixed 叠加，不再挤动内容。
- 悬浮球左右停靠安全间距从 12pt 增至 18pt。
- 从错题详情进入待复习时，目标卡片滚动到队列中央并短时高亮。
- 大列表使用稳定项目记忆化、延迟搜索、渲染隔离和屏外跳过，降低缩放与操作时的重复布局、绘制和 React 渲染负担。

## 验证

- 宽、窄窗口下完成错题本、筛选弹层、详情状态胶囊和待复习跳转的浏览器可视复验。
- TypeScript 类型检查、自动化测试与生产构建结果见逐轮变更记录。
- 原生同步关闭提示、悬浮球边距和千题级列表性能仍需安装包真机复验。
