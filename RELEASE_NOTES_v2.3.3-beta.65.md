# v2.3.3-beta.65

## 错题详情 UI 重构（2026-09-03）

- 题目标题与上方小字来源保持原样；标题行右侧居右对齐一组状态：等级胶囊（复用全局等级控件观感，点击调整等级）· 放大的分隔点 · 右侧上下叠放剩余天数胶囊（按到期状态取对应色系）与灰底添加日期胶囊，两者上下缘与等级胶囊对齐。悬浮操作条内控件不再各自带按钮样式，仅以空隔排布，选中项统一使用强调色底，取消错题与定位原题为纯图标（取消错题采用 Phosphor 垃圾桶图标，点击动画过渡为对勾、再次点击才删除）；条内文字与图标尺寸放大一档。
- 标签栏紧随标题行并延伸到面板最右，以芯片形式展示自定义标签，点击弹出原有标签编辑器（勾选/删除/新建），无标签时显示"＋ 标签"。
- 其余控件整合为悬浮于预览窗口上的圆角操作条：题目、答案、收藏、定位原题、取消错题、折叠，从左到右排列；条内控件不再各自带按钮样式，仅以空隔排布，选中项统一使用强调色底（accent tint），取消错题为红色文字。
- 操作条支持跟手拖动、自动吸附预览窗口四边；停靠上下边时横向排布、左右边时纵向排布；折叠后收为一个圆点，点击圆点展开；停靠位置与折叠态本地记忆。
- 全部取色沿用现行 rd-* token 与三档等级色系；同步清退旧标签行/折叠钮/meta 行等 126 个退役样式选择器。
- 版本迭代为 `2.3.3-beta.65`。

验证：`pnpm check`、159/159 Node 自动化测试、生产构建通过；预览截图三张（默认横排 / 右侧竖排+标签弹层 / 折叠圆点）存于 iCloud 同步文件夹 `错题详情UI预览/`。拖动手感与 Pointer Events 行为需真机复核。

完整记录见 `docs/changes/2026-09-03-beta65-detail-ui-refactor.md`；后续优化方向见 `docs/handover-detail-ui.md`。
