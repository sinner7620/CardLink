# v2.3.3-beta.59

## UI 阶段二上下半程（2026-09-02）

- 上半程完成错题页与公共控件的模块内部级联收敛。
- 下半程完成待复习、设置、概览与 Shell 的模块内部级联收敛；导出页保持可见设计不变。
- 六个阶段二目标模块删除 13 条同选择器、同上下文、同优先级下必然被后值覆盖的历史声明。
- 八个 UI 模块以 `mn-ui-base / mn-ui-priority` 两个明确的 cascade layer 替代历史声明级强制覆盖，源码和生产 CSS 的 `!important` 从 1345 处降为 0。
- 新增阶段二 Playwright 可视回归，覆盖 500px/920px 的总览、错题、待复习和设置页面，并检查页面级横向溢出。
- 版本迭代为 `2.3.3-beta.59`。

验证：`pnpm check`、158/158 Node 自动化测试、2/2 Playwright 可视测试和生产构建通过；CSS 转换检查点的 8 组截图哈希完全一致。最终 beta.59 中 6 组非设置页截图仍完全一致，设置页仅版本文字由 beta.58 更新为 beta.59。

完整记录见 `docs/changes/2026-09-02-beta59-ui-stage2-complete.md`。
