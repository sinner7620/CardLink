# v2.3.3-beta.91：候选条恢复原生答案选择弹窗

日期：2026-09-06。基线：beta.90（正式渠道，版本号 2.3.3-beta.91）。

## 实现

- 对照用户提供的 `v2.3.3-beta.61.mnaddon`，恢复当时基于 MarginNote `select(...)` 的原生多答案选择弹窗。
- 点击答案窗口候选条后，弹窗标题显示“找到 N 个答案”，提示“请选择要展示的答案卡片”。
- 每个选项继续展示 beta.61 的信息结构：倒序祖先路径、答案标题、`★标准答案` 标识以及最多 42 字的答案正文摘要。
- 删除 beta.86—90 的自绘 UIView 下拉面板、候选行 UIButton、tag 行序回退和相关事件回调，只保留候选长条作为原生弹窗入口。
- beta.90 的居中对齐、答案卡去重和高置信唯一结果隐藏候选条逻辑保持不变。

## 兼容性与数据影响

不改变绑定、答案索引或错题数据；正式插件 ID 与 stable 构建渠道不变，可覆盖升级。

## 验证

- `pnpm check` 通过。
- `pnpm test` 180/180 通过；候选交互回归断言已更新为 beta.61 原生弹窗结构，并确认自绘下拉代码不存在。
- `pnpm build` 通过，生成 `dist/mn4-answer-matcher-v2.3.3-beta.91.mnaddon`。
- 已复制至 `E:\iCloudDrive\同步文件夹\mn4-answer-matcher-v2.3.3-beta.91.mnaddon`；源包与副本 SHA-256 均为 `D106D3BB75D48DC71F3426799B17241A0A96D1EE046043ADFE96C11CE1CB7D3C`。

## 未验证限制

MarginNote 原生 `select` 弹窗的真机呈现与选择后切换答案仍需 iPad 复验。
