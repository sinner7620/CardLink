# CardLink v2.4.1b10

## 界面改进

- 查找答案窗口与插件页的关闭、刷新控件改为参考图样式的 `96×40pt` 连续圆角胶囊。
- 两个按键各占一半点击区，中间不再显示分割、间距或独立按钮底色。
- 点击任一按键会带动整枚胶囊做果冻式压缩、过冲与回弹；系统“减弱动态”开启时停用动画。
- 多答案候选出现时，查找答案窗口按同一视觉规则扩展为三键胶囊。

## 兼容与数据影响

- 关闭、刷新、候选选择与左右换边功能保持不变。
- 不修改任何用户数据或存储结构，无需迁移。
- 保持正式插件 ID、标题和 `stable` 构建渠道。

## 验证

- `pnpm check`：通过。
- `pnpm test`：233/233 通过。
- Playwright `900×640` 实际渲染确认 `96×40px` 胶囊、等宽双槽与按压动画均生效。
- `pnpm build`：通过；存在一条与本轮无关的既有 CSS 选择器警告。
- 安装包：`CardLink-v2.4.1b10.mnaddon`，576,324 bytes。
- SHA-256：`BC283B38CA27C9BFECC70E468AAB336163CD7B770769EEC02C0EF2F3E52F4B2B`。
- 已复制至 `E:\iCloudDrive\同步文件夹\CardLink-v2.4.1b10.mnaddon`，交付副本哈希一致。

## 未验证限制

- 查找答案窗口的原生果冻回弹仍需在用户 iPad 的 MarginNote 4 中做最终手感确认。
