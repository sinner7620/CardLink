# v2.3.3-beta.68

- 题目/答案预览统一入口与手势控制器，删除重复缩放；原生查找答案和Web预览复用同一实现。导出继续使用静态排版。
- 固定浅色预览；独立手写按坐标尺寸显示，取消拉满容器和最小宽度放大。
- 标题/标签左列对齐，32px等级胶囊居于两行中间；折叠筛选栏上下间隙统一。
- 菜单与侧边按钮开关直接使用JS服务，Bridge显式传入所属addon；移除宿主自定义boolean方法，调整点击回调与菜单外关闭控件。
- 启动及桥接减少重复序列化、避免隐藏rAF阻断续传，外壳提交后才撤启动提示；开关就地更新状态。

验证：TypeScript和173项测试通过；浏览器检查浅色、尺寸、缩放、开关与布局。原生闪退/冷启动实际耗时仍待真机确认。

长列表虚拟化、ReviewCard隔离、独立预览层及按需接口尚未实施，需审批调查方案。
