# CardLink 2.4.1-b7

- 修复 AI 配置页控件形状失调：switch 开关恢复标准 iOS 形状（含此前不可见的关闭态灰色轨道），行布局与按钮不再被旧基础样式打散。
- AI 服务改为单入口：下拉选择分析错题 AI 后，仅展开当前选中服务的配置编辑，不再同时列出 OpenAI 和 DeepSeek；入口形式与题目识别（OCR）一致。
- 导出快速预览按固定 60% 缩放显示 A4 版面（此前 1:1 过大）。
- 「启用题目识别」提示更正为"关闭后图片题不会发送，文字题仍直接读取"。

已在浏览器预览中截图自查开关、行布局、编辑器展开与下拉切换；MarginNote 真机视觉仍待验收。桌面类型检查、256 项自动化测试及构建通过。
