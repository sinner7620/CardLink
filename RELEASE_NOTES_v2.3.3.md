# v2.3.3

## 正式版更新

- 将当前 beta 工作树及 beta.73 最终包中的列表结构改动同步为正式版，插件 ID、标题、版本号与设置键切换到正式渠道。
- 错题列表与预览区恢复贯通布局，并为列表项选中边框和预览分割线增加内缩留白。
- 错题筛选悬浮条改为半透明毛玻璃，压缩并垂直居中搜索框，移除等级筛选与批量改级的原生底框。
- 修复详情页纵向等级/剩余天数组合被裁切，以及标签只显示首字的问题。
- 详情预览悬浮工具条调整为 50% 背景透明度与 1px 毛玻璃。
- 待复习的定位、答案、历史和三档结果始终保持同一行；窄窗口按历史、答案、定位的顺序隐藏文字，仅保留图标。
- 修复从错题本定向跳转后普通进入待复习仍定位旧题、目标居中受虚拟布局影响的问题。
- 修复插件拖到屏幕顶部后 WebView 自动增加顶部 inset，并恢复原生插件面板四周阴影。
- 为缺少专用状态的可点击 Web 控件补齐统一中性悬停反馈。

## 验证

- 运行 TypeScript 类型检查、自动化测试和正式生产构建。
- 核对最终 `mnaddon.json` 使用正式插件 ID、正式标题与 `2.3.3` 版本号。
- 后续修复见 `RELEASE_NOTES_v2.3.3-beta.72.md`。
