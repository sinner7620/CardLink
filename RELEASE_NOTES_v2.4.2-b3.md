# CardLink v2.4.2-b3

## 修正说明

- 双击目标修正为题目卡片旁的“查找答案”按钮，不再监听脑图卡片双击。
- 单击“查找答案”继续打开原有 CardLink 答案窗口；双击该按钮才调用 MarginNote 原生卡片源浮窗定位答案卡片。
- 单击操作等待双击判定完成；识别到双击后取消待执行单击，避免两个答案窗口同时出现。
- 第一次轻点导致 MarginNote 原生卡片菜单关闭时，侧边按钮会保留到双击判定结束，保证第二击仍能命中按钮。
- 保留缺失答案确认生成、唯一同层级分支复用和重复生成保护；已有唯一同分支时不会复制新分支。

## 数据与兼容性

- 保持 `mnChannel: "stable"`、正式插件 ID `marginnote.extension.mn4-answer-matcher` 和标题 `CardLink`。
- b1、b2 已写入交付目录，不做同版本覆盖；包含完整双击交互边界修正的最终包递增为 v2.4.2-b3。

## 验证与限制

- `pnpm check`、`pnpm test`（269/269）和 `pnpm build` 均通过。
- 安装包大小为 583,677 bytes；项目产物与 iCloud 交付副本的 SHA-256 均为 `6fbe9649390f2e6f32aafd441bd88093b1f3da0549f09b73eaffe0c1b2a91bc3`。
- 已核验包内版本 `2.4.2-b3`、正式插件 ID、标题、按钮单双击入口及原生浮窗接口；旧的脑图卡片双击入口不存在。
- 构建仍报告既有 `.sfIconGlyph:svg` CSS 选择器警告，本次未修改该样式，产物正常生成。
- 原生浮窗定位及按钮单双击交互仍需 MarginNote 4 真机验收。
