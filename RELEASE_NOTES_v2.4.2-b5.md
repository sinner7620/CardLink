# CardLink v2.4.2-b5

## 修正说明

- 优化“查找答案”按钮的单击响应：双击识别器不再额外扣留触摸事件，手动判定时间由 360ms 缩短为 300ms。
- 双击定位前保存已显示的原生卡片源浮窗位置，并在聚焦后恢复；同一答案已经显示时跳过重复聚焦，减少浮窗无意义刷新。
- 未找到答案的第一层弹窗现在显示唯一的“取消”，并同时提供“刷新索引后重试”和“生成答案卡片”。
- 选择生成时会先静默刷新索引：若发现已有答案则直接定位；确认仍无答案才生成，不再二次确认。
- 生成答案卡片仍复用唯一同层级分支，不重复复制已有分支。

## 数据与兼容性

- 保持 `mnChannel: "stable"`、正式插件 ID `marginnote.extension.mn4-answer-matcher` 和标题 `CardLink`。
- 浮窗位置恢复只在能够安全识别原生浮窗容器时执行；识别失败时不修改宿主布局，答案定位仍照常进行。
- 原生浮窗接口和实际按钮触感仍需 MarginNote 4 真机验收。

## 验证与限制

- `pnpm check`、`pnpm test`（269/269）和 `pnpm build` 均通过。
- 安装包大小为 584,331 bytes；项目产物与 iCloud 交付副本的 SHA-256 均为 `27609c80d7d03c3d19f842d16371549c331b7cf0e4521366e6b9914b87b0fef3`。
- 已核验包内版本 `2.4.2-b5`、正式插件 ID、标题以及触摸延迟、浮窗位置追踪、原生聚焦、第一层两个操作入口和修正后按钮索引。
- 构建仍报告既有 `.sfIconGlyph:svg` CSS 选择器警告，本次未修改该样式，产物正常生成。
