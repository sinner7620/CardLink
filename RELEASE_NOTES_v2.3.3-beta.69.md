# v2.3.3-beta.69

## 修复

- 删除 beta.64 新增且仅供悬浮球使用的自绘原生快捷菜单；悬浮球单击直接复用主面板原有显示/隐藏入口，长按仍复用窗口复位入口。
- 定位原题不再设置整页加载状态，也不再让用户误以为正在重载全部错题。
- 面板重新显示时先比较轻量错题修订号；数据未变化不重新建立分页快照。
- 全量刷新串行合并，避免面板显示、原生通知和手动刷新竞争分页传输。
- 待复习题目详情请求去重、最多双并发，并以 6 题或 8 MB 为上限缓存近期预览。
- 答案候选和缩放状态进入独立预览组件；缩放不再更新整列状态，离屏题目跳过浏览器布局和绘制。
- `mistakeDetail` 运行日志拆分记录答案查找、答案 HTML 和原题 HTML 耗时。

## 保留行为

- 根据用户要求，本版没有改变“当前题目一次返回其全部候选答案”的协议。
- 题目和答案继续使用同一个 `CardPreview` iframe 与手势控制器。

## 验证边界

- 自动测试、类型检查、构建和桌面浏览器交互均通过。
- 悬浮球属于 MarginNote 真机原生入口，仍需安装本版真机验证；没有 `.ips` 崩溃报告时不能确认 beta.68 的具体原生异常栈。
