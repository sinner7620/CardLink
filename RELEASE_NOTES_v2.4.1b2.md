# CardLink v2.4.1b2

## 新增

- AI 配置页新增“错题题目准备”，可选择一个学习集并逐题执行 OCR。
- 每道错题会先把完整原题卡片渲染成单张 JPEG，再发送至 MinerU `vlm` 模型识别。
- 右侧实时显示当前上传的题目卡片图片、单题 OCR 进度、总进度与 OCR 返回文本。
- 任务结束后显示题目总数、成功数和失败数；单题失败不会阻断后续题目。

## 行为与数据调整

- OCR 题目文本不写入错题主库，改为按错题 ID 独立保存在 `MNAnswerMatcher/ai/content/records/`。
- 每份本地题目快照记录来源、整卡图片 SHA-256 内容指纹、OCR 服务与模型、处理时间和识别文本。
- AI 错题总结不再临时上传题目图片，只读取已准备的本地题目文本；尚未准备的题目会计入内容不可用。
- 清理 OCR 缓存时会同时清理已准备题目文本，缓存统计新增已准备题目数量。

## 兼容性

- 正式插件 ID、标题与 `mnChannel: "stable"` 保持不变。
- 既有错题、复习记录和报告结构不迁移；新增内容仓库与错题主库隔离。

## 验证

- `pnpm check`：通过。
- `pnpm test`：通过，222/222。
- `pnpm build`：通过。
- 包内清单已核对：正式插件 ID `marginnote.extension.mn4-answer-matcher`、标题 `CardLink`、版本 `2.4.1b2`。
- 构建包与交付副本均为 523,678 字节，SHA-256 一致：`F57FBB62731E0AC300151278C70C392B38465EBBF6214073F4142D95DA142758`。

## 未验证限制

- MinerU 的真实上传、轮询及 OCR 质量仍需在配置有效 Token 的 MarginNote 4 真机环境中验证。
- 超长、超大或包含远程跨域资源的卡片，仍可能受 WebView 画布尺寸、内存和资源加载限制影响。
