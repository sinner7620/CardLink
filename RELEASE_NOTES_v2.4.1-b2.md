# CardLink v2.4.1-b2

## 更新说明

- 修复修改原卡标题后，详情与列表不同步的问题。详情、待复习原题或答案读取到新标题后，同步更新共享选题列表；未读取的卡片需打开预览或刷新索引。
- 修复题目和答案预览缩小后贴左上角的问题，有剩余空间的方向居中，长内容仍可滚动。
- 题目预览支持双击或双轻点显示、隐藏脑图绑定手写。
- 补齐摘录图片的笔迹叠加，并让配对图片与笔迹使用相同底图坐标和比例。
- 包含 b1 的已删除原卡片取消错题修复。

## 兼容性与限制

- 无数据结构迁移，不改变 AI 上传设置或原始笔迹。脑图绑定手写在独立区域展开，不还原脑图上的相对位置。
- 图片叠加依赖明确配对的 paint/drawing 数据；真实卡片的笔迹位置及不同绑定方式仍需 MarginNote 真机复验。
- 保持 `stable` 渠道、ID `marginnote.extension.mn4-answer-matcher` 和标题 `CardLink`。

## 验证与交付

- `pnpm check`、`pnpm test`（243/243）、`pnpm build` 通过。构建保留既有 `.sfIconGlyph:svg` CSS 警告。
- 包内版本和插件身份已核验，安装包为 577,849 bytes。
- 已复制到 `E:\iCloudDrive\同步文件夹\CardLink-v2.4.1-b2.mnaddon`。
- 复制件与 dist 原包 SHA-256 一致：`CE0856DED4C41CBFA12348028FB1A820BD285FB3A3E13B491388AFD9C2369021`。
