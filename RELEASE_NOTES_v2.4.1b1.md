# CardLink v2.4.1b1

## 修复

- 修复待复习页“展开全部题目”时，部分卡片长期停留在“正在读取完整原题”的问题。
- 原题批量展开改用轻量读取，不再为每一道题同步执行答案匹配和全部答案 HTML 生成。
- 当前展开的原题不再受 6 项请求缓存上限影响；展开超过 6 道时，先加载的卡片不会被淘汰回加载状态。
- 修复筛选、折叠或列表刷新期间在途原题回包被丢弃后无法重新落入页面状态的竞态。
- 单题读取失败时显示明确失败状态和“重试”按钮；失败任务会释放并发槽位，不再阻塞后续题目。

## 兼容与数据影响

- 保持正式插件 ID、标题、稳定渠道及既有错题/复习数据格式不变。
- 新增内部桥命令 `mistakeQuestion`；完整详情与答案仍使用原有 `mistakeDetail`，不改变持久化数据。

## 验证

- `pnpm check`：通过。
- `pnpm test`：通过，220/220。
- `pnpm build`：通过。
- 包内清单已核对：正式插件 ID `marginnote.extension.mn4-answer-matcher`、标题 `CardLink`、版本 `2.4.1b1`。
- 构建包与交付副本均为 519,198 字节，SHA-256 一致：`7EF8C96650174FC571C2C2845A874D3A66B73A7B3B1B3AC23A1E61A12139A411`。

## 未验证限制

- 尚需在 MarginNote 4 真机上用包含大量复杂题卡、图片和手写内容的真实待复习队列复验批量展开体验。
- 构建仍可能报告既有的 `.sfIconGlyph:svg` CSS 选择器警告；该警告不影响产物生成。
