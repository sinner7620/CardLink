# v2.3.3-beta.70

## 修复与重构

- “刷新错题分类索引”以原卡 `notebookId` 重建来源身份、路径、分类与答案绑定；来源移动导致 recordId 变化时合并重复记录，并保留收藏、复测历史和计划。
- 定位原题改为单一路由状态机：跨学习集只发送一次官方 MN4 链接，以真实焦点/选中状态确认成功；官方聚焦失败后才恢复 UI 状态，同步开关不再造成成功跳转的误报失败。
- 定位超过 450ms 时提示正在切换；请求完成前按钮锁定，成功后才播放 map-pin → check 动画。
- 共享卡片缩放控制器忽略同一比例的重复回写，消除手势控制器与 React 工具条双写造成的明显抖动。
- 启动首屏使用轻量持久快照立即恢复，并在后台分页校验；原生工作台聚合按数据修订号缓存，降低面板冷启动和重显等待。
- 错题本移除列表、详情外框，改为同底层竖线分栏；列表上下贯通。搜索、多选与筛选合并为 sticky 毛玻璃悬浮条。
- 详情悬浮条补足半透明毛玻璃背景和侧边间距；空间收窄时按“删除、定位、答案、题目”逐级隐藏文字。
- 自定义标签与加号统一使用主蓝色；标签达到三行时，等级与剩余天数组合胶囊改为纵向拼接。

## 验证

- TypeScript 类型检查、自动化测试和生产构建通过。
- 原生跨学习集延迟、首次安装无缓存启动耗时及真机触控仍需安装包实测。
