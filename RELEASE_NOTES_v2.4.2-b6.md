# CardLink v2.4.2-b6

## 新增与修正

- “查找答案”恢复零等待单击；原双击行为改为长按，匹配成功后使用真实答案 `noteId` 打开 MarginNote 原生卡片编辑器。
- 未匹配答案时，单击与长按共用同一确认框。“生成答案卡片”不再刷新索引，直接写入当前题目脑图所绑定的答案脑图。
- 生成答案时会复用唯一对应父节点；没有对应父节点时复制题目的直接父节点。已有父节点不会再次生成新分支。
- 待复习队列新增“复习模式”：关闭插件页、定位队列第一题，并在脑图区顶部显示原生悬浮工具条。工具条包含序号选择、上一题、下一题、错题信息和退出报告。
- 同一学习集内的多个题目脑图固定使用独立绑定范围，可分别绑定不同答案脑图，互不覆盖。
- 插件面板原生拖动与缩放手势增加 iPad 蓝牙鼠标/触控板的间接指针输入支持。

## 数据与兼容性

- 保持正式渠道、插件 ID `marginnote.extension.mn4-answer-matcher` 与标题 `CardLink`。
- 旧版保存的“按整个学习集绑定”关闭状态会自动升级为按子脑图独立绑定；旧笔记本级绑定仍可作为尚未逐脑图重绑时的兼容回退。
- 新建答案卡不会自动进入现有答案索引；答案填写完成后，可按需手动刷新答案索引。
- 原生编辑器、跨学习集定位、卡片复制/父子挂接及外接鼠标手势仍需 MarginNote 4 真机验收。

## 验证与交付

- `pnpm check`、`pnpm test`（272/272）和 `pnpm build` 均通过。
- 安装包大小为 585,537 bytes；项目产物与 iCloud 交付副本的 SHA-256 均为 `6d3a4740c1bdf340bef13048874b6ab47faa0b6ebb2b5b46aa7db582283606cf`。
- 已核验包内版本 `2.4.2-b6`、正式插件 ID 与标题。
- 构建仍报告既有 `.sfIconGlyph:svg` CSS 选择器警告，本次未修改该样式，产物正常生成。
