# v2.3.3-beta.64

## 插件全局开关与悬浮球快捷设置（2026-09-03）

- 设置 → 插件新增「插件全局开关」：关闭后暂停弹出菜单监听、定时提醒、答案工具条与答案窗口；悬浮球、主面板与设置入口保留；NSUserDefaults 持久化，重启后保持，无历史配置默认开启。
- 悬浮球单击改为打开「CardLink 快捷设置」自绘小界面（对齐插件 UI 规范：白底圆角卡片、阴影、44pt 行高），提供四项：插件全局开关（显示当前状态）、重置窗口位置、关闭按钮位置（左/右）、打开/关闭主面板；点击菜单外关闭。所有动作复用现有实现，不新增第二套业务逻辑。
- 修复 v61 缺陷：主面板隐藏时，长按悬浮球现在仍能复位答案窗口（两个窗口分别判断、互不阻断）。
- 修复错题本跨脑图定位原题后列表刷新乱跑：跨学习集跳转不再触发多余的面板刷新，且任何整页刷新都会保持错题列表的滚动停留位置。
- 版本迭代为 `2.3.3-beta.64`。

验证：`pnpm check`、159/159 Node 自动化测试、生产构建通过；安装包内容已抽验（快捷菜单选择器、全局开关持久化、Bridge 命令与 dashboard 附加字段）。功能与观感需真机按方案验收清单（A–K）复核。

完整记录见 `docs/changes/2026-09-03-beta64-global-switch-and-quick-menu.md`。
