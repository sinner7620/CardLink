# v2.3.3-beta.94

基线：beta.93。本轮修复错题工作台并发分页初始化互相覆盖导致加载停在首批 25 条的问题，并把查找答案窗口的关闭、刷新、候选答案三个控件合并为插件风格的悬浮工具条。

## 修复与调整

- 错题分页传输由单一全局快照改为按 `transferId` 隔离的传输集合；仪表盘与错题列表同时初始化时，各自可以继续读取后续分页。
- 分页传输最多保留 4 份，并按最近使用时间执行 60 秒过期与最久未使用淘汰，避免快照长期占用内存。
- 答案窗口顶部改为统一的半透明圆角悬浮条，关闭、刷新、候选答案三个控件共享同一容器并保持中线对齐。
- 悬浮条跟随“答案窗口关闭按钮位置”开关整体左右换边；右侧时同步镜像控件顺序。
- 只有两个及以上有效候选答案时才显示候选控件；精确收敛为唯一答案时，悬浮条自动缩为关闭和刷新两个控件。
- 候选答案仍复用 MarginNote 原生选择弹窗，不使用网页下拉框。

## 兼容性与数据影响

不修改错题记录、答案绑定、设置键或桥接协议；升级后无需迁移数据。插件继续使用正式渠道 ID 与 `mnChannel: "stable"`。

## 验证

- `pnpm check` 通过。
- `pnpm test` 通过，182/182；包含双传输并发续页、悬浮条左右镜像和原生候选弹窗静态回归测试。
- `pnpm build` 通过，生成 `dist/mn4-answer-matcher-v2.3.3-beta.94.mnaddon`。
- 安装包已复制到 `E:\iCloudDrive\同步文件夹\mn4-answer-matcher-v2.3.3-beta.94.mnaddon`；源包与副本 SHA-256 均为 `2F5F3831109FAF7854D25A5898107F9277287894E0D13B3882426120FA78AA0E`。

## 未验证限制

- 尚未在包含约 880 条错题的真实 iPad 数据上完成冷启动全量翻页验证。
- 悬浮条的真机触控、阴影观感、左右开关位置及 MarginNote 原生候选弹窗仍需在 iPad 上复验。
- 构建保留既有 `.sfIconGlyph:svg` Lightning CSS 警告；本轮未修改对应设置页样式，且不阻止构建。
