# v2.3.3-beta.90：查找答案候选条修复

日期：2026-09-06。基线：beta.89（正式渠道，版本号 2.3.3-beta.90）。

## 修复

- 候选条改为与 44pt 关闭/刷新按钮共用 26pt 垂直中心线，不再出现视觉偏上。
- 修复点击无反应的完整原因：候选展开与选项点击回调此前没有注册到 Rails 插件实例；透明拖拽层还覆盖了候选按钮；下拉选项也没有实际 frame。现已注册两个回调、把候选控件提升到拖拽层之上，并为每行设置可点击布局。
- 候选数按答案卡 `noteId` 去重，不再把同一卡片的多个脑图节点重复计数。
- 仅在结果确有歧义时显示候选条。唯一答案、唯一祖先路径最佳答案，以及并列最佳中唯一带“标准答案”标签的答案都会直接显示，不再给出候选入口。

## 兼容性与数据影响

不改变绑定、索引或错题存储结构；正式插件 ID 与 stable 构建渠道不变，可原地覆盖升级。

## 验证

- `pnpm check` 通过。
- `pnpm test` 180/180 通过，新增候选去重、路径唯一胜出、唯一标准答案和真实歧义保留候选的行为测试。
- `pnpm build` 通过，生成 `dist/mn4-answer-matcher-v2.3.3-beta.90.mnaddon`。
- 产物已复制到 `E:\iCloudDrive\同步文件夹\mn4-answer-matcher-v2.3.3-beta.90.mnaddon`；源包与副本 SHA-256 均为 `1F68FF437CE1A0032AA9BB4FB4A335B4040162FB66834385E4E0AB861631DB5C`。

## 未验证限制

原生 UIButton/UIView 的最终触摸命中和视觉位置仍需在 MarginNote iPad 真机复验；桌面构建环境无法启动该宿主窗口。
