# CardLink v2.4.1b5

## 新增

- AI 设置的发送内容新增“脑图绑定手写”独立开关，默认关闭，旧配置升级后不会自动外发脑图笔迹。
- 开启后，错题题目准备会按学习集 ID 和原题卡片 ID 调用 MarginNote 官方脑图草稿接口，读取绑定到该卡片的手写内容。
- 绑定手写会追加到整张原题卡片中，由现有 PencilKit/PKDrawing 渲染器绘制后，与题目内容一起发送至所选 OCR 引擎。

## 兼容与容错

- 每次题目准备任务固定启动时的“脑图绑定手写”选项，任务进行中修改设置不会让同一批题目混用内容范围。
- 优先使用原始 PencilKit 笔迹；没有原始笔迹但存在官方栅格媒体时才使用图片回退，避免重复渲染同一份内容。
- 区分“没有绑定手写”“当前 MarginNote 不支持读取”“媒体数据不可读”，这些状态会显示在单题进度下方，且不会阻断原题 OCR。
- 保持现有卡片评论手写、OCR 文本快照和错题数据结构兼容。

## 验证

- `pnpm check`：通过。
- 定向测试：通过，51/51。
- `pnpm test`：通过，225/225。
- `pnpm build`：通过；保留现有 `.sfIconGlyph:svg` 非法伪类警告，不影响产物生成。
- 正式构建产物：`dist/CardLink-v2.4.1b5.mnaddon`，572,791 字节。
- 交付副本：`E:\iCloudDrive\同步文件夹\CardLink-v2.4.1b5.mnaddon`。
- 两份文件 SHA-256 均为 `B4E47AA8F6A671D007C8F24FB9DB8F248058FCE65B7432123B7F0A282006D3D6`。
- 包内清单确认：正式插件 ID `marginnote.extension.mn4-answer-matcher`、标题 `CardLink`、版本 `2.4.1b5`。

## 未验证限制

- 尚需在 MarginNote 4 真机上验证不同绑定方式（自动绑定、焦点绑定、手动绑定）返回的草稿媒体结构与最终渲染效果。
