# v2.3.3-beta.93

基线：beta.92。按真机反馈把答案窗口的候选长条改为**候选圈**：30pt 圆形、仅显示当前是第几个候选答案的数字，位置仍在刷新按钮旁（跟随关闭/刷新的左右侧设置），点击行为不变（MarginNote 原生选择弹窗列出候选）。

## 实现

- 候选控件从长条（96–210×30）改为 **30pt 圆形**（圆角 15 = 正圆，深色半透明胶囊同体系），标题仅为当前候选序号数字。
- 布局跟随侧边：左侧布局时紧邻刷新按钮右侧（x=106）；右侧布局时贴在刷新按钮左侧。
- 清理内嵌下拉面板的残留引用（当前设计点击即原生弹窗，无内嵌下拉）。

## 影响文件

`src/answer-card-view.ts`、`package.json`（版本号）。

## 兼容性与数据影响

数据结构、存储键、桥接命令不变；正式渠道 ID 原地覆盖升级。

## 验证

- `pnpm check` 通过；`pnpm test` 180/180 通过（断言更新为候选圈形态：circleX/circleY、仅数字标题）；`pnpm build` 通过，生成 `dist/mn4-answer-matcher-v2.3.3-beta.93.mnaddon`，已拷贝至 `E:\iCloudDrive\同步文件夹\`（SHA-256 见变更记录）。

## 未验证限制（必须真机）

候选圈显示位置、点击弹窗、序号随切换更新的实际表现。
