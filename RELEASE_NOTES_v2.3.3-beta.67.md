# v2.3.3-beta.67

- 悬浮条显著透明化、定位图标统一；窄窗口保持单行，空间不足隐藏题目/答案文字；单答案不显示数量，多候选闭合只显示序号。
- 拖动过程中实时横竖转向；折叠展开改为同一容器连续动画，保留原停靠记录。
- 标签对齐标题、缩小间距；Plus→ChevronDown 使用既有 Morphicons 内核与 Lucide 数据；等级胶囊和标题同高。
- 修复开关发送当前值的问题，改为只控制卡片侧边按钮，清除旧全局后台启停代码。
- 日志直接写临时文件并调用系统保存，移除人为等待及点击时原生选中视图采样。
- 同一面板预载，启动不依赖隐藏 WebView 的 rAF；窗口归属和尺寸判断统一至 UIWindow，避免选卡临时布局导致漂移。
- 移除悬浮球路径中的宿主窗口写入、菜单/面板原生阴影属性赋值；长按不复位隐藏窗口。
- 删除重复详情标题样式和旧定位图标小尺寸覆盖，收紧折叠筛选栏间距。

验证：TypeScript及167项测试通过；浏览器验证多答案、开关、窄屏布局、实时转向与动画中间帧。

**仍需真机验收：悬浮球闪退、系统日志保存耗时、冷启动、原生窗口位置。缺少崩溃报告，尚不能确认闪退的唯一根因或宣称已彻底消除。**

完整记录：docs/changes/2026-09-03-beta67-device-feedback.md。
