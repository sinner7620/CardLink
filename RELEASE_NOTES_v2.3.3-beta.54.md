# v2.3.3-beta.54

## 错题页按钮统一（2026-09-02）

- 批量“导出”删除独立主蓝覆盖，复用现有次级按钮体系，与同栏操作保持一致。
- 详情“题目/答案”未选中态复用“定位原题”的底色、边框和尺寸 token；选中态及危险操作语义保持不变。

完整记录见 `docs/changes/2026-09-02-button-style-unification.md`。

## 答案窗口位置同步与 CSS 阶段一（2026-09-02）

- “插件窗口关闭按钮”左右位置设置会立即同步到已存在的答案窗口；关闭状态不会因此打开或复位。
- 答案窗口关闭、刷新按钮和拖拽区改为共用一套原生布局，关闭/刷新按钮共用同一按钮工厂，不再维护重复坐标与外观逻辑。
- Web 样式从 `panel.css + panel-preview.css` 覆盖链拆为 tokens、controls、shell、overview、mistakes、review、export、settings 八个所有权模块；旧覆盖文件和确认无引用的实验样式已删除。
- 同选择器冗余声明完成第一轮机械去重，生产 CSS 降至 150.02 KB；后续 `!important` 收敛将在各页面模块内部继续进行。

完整记录见 `docs/changes/2026-09-02-answer-window-side-and-css-stage-1.md`。验证：类型检查、154/154 自动化测试和生产构建通过，并完成 500/620/720/920px Playwright 回归；答案窗口即时重排仍需 MarginNote 4 真机复验。

## 真机反馈第四轮修复（2026-09-02）

- 待复习原题恢复单指纵向滚动；三档结果按钮换行时，队列内每一道题都会等宽铺满整行。
- 悬浮球恢复开/关切换并在点击时重绑当前窗口；关闭态长按不再复位或打开窗口，可见答案窗口才会随插件面板一起复位。
- 总览进度条跳过 0 计数分段，剩余颜色始终保留首尾圆角。
- 收藏星移到自定义分类前；窄窗列表首行固定为多选、数量、搜索和折叠，不再错行或换序。
- 答案窗口新增刷新按钮；答案窗口与插件页的刷新均同时恢复各自默认位置和尺寸。
- 收藏动画直接使用 vanilla `lucide` 的 `Star`、`Check`、`X` IconNode 交给 Morphicons，删除收藏专用手写路径。

完整记录见 `docs/changes/2026-09-02-beta54-feedback-round-4.md`。验证：类型检查通过，测试 152/152 通过，生产构建通过，并完成 500px/620px Playwright 窄窗与 iframe 滚动属性回归；悬浮球及原生答案窗口仍需 MarginNote 4 真机复验。

## 真机反馈第三轮修复（2026-09-02）

- 收藏恢复旧版 Morphicons 星星→对号/X→星星的 520/480ms 形变时序。
- 错题工具栏计数改为“共x道 / x/x”，“修改等级”替代“批量改等级”，并移除计数与搜索框之间的无效留白。
- 标题与下方两行统一左基线，收藏星移到标题左侧；标签与剩余天数用 `·` 紧邻分隔。
- 总览进度条恢复圆角；待复习三档按钮与左侧操作控件同高，答案视口放大到 300–520px，时间轴启用局部触摸横向滚动。
- 完成态统一显示“已结束”。
- 悬浮球单击强制显示插件页面，长按先显示面板再同时复原插件面板和答案窗口。
- `src/ui-tokens.ts` 成为 Web、原生入口和卡片 HTML 的颜色唯一值源：主蓝 `#0e8dfd`、灰底 `#d8d8dd`，三档色沿用插件现有值。
- HTML 转义与双指缩放事件管线分别收口为单一共享实现。

完整记录见 `docs/changes/2026-09-02-beta54-feedback-round-3.md`。验证：类型检查通过，测试 150/150 通过，生产构建通过，并完成 Playwright 页面回归；悬浮球和 iPad 触摸时间轴仍需 MarginNote 4 真机复验。

## 真机反馈第二轮修复（2026-09-01）

- 恢复可见的多选批量改级，并把多选控件移到题目计数前；搜索框右侧新增筛选折叠。
- 错题列表改为“标题与等级 / 标签与剩余时间 / 来源”三行，统一标题起点和等级胶囊尺寸。
- 恢复详情收藏星形图标及收藏/取消形变动画，放大详情标题。
- 待复习答案改为局部加载，消除全局闪烁；缩放内容不再撑高预览窗口。
- 恢复总览三色进度条发光，复测时间轴横向滚动限制在历史区域内。
- 已结束复习的详情不再显示剩余天数；三档托管标签不再混入自定义标签。
- 三档复习结果按钮统一尺寸和颜色。
- 根据真机日志修复同步关闭提示：前端 `showLocateHint` 未传入导致的异常已消除。
- 修复悬浮球长按 selector 未注册；点击入口不再限制脑图场景，并强制底层按钮使用 72% 透明度。

完整记录见 `docs/changes/2026-09-01-beta54-feedback-round-2.md`。验证：类型检查通过，测试 150/150 通过，生产构建通过，并完成 Playwright 页面回归；悬浮球和同步提示仍需 MarginNote 4 真机复验。

## P0/P1 收口修复（2026-09-01）

- 裸 `不会`、`不熟`、`掌握` 标签恢复为用户标签，不再被错题系统误识别或删除。
- 保留 Beta 与正式版共享存储；正式版首次启动时提醒不能与 Beta 版同时启用。
- 错题标签写入增加可验证的三态结果，无法回读时不再乐观提交本地状态。
- 收藏改为按 `recordId` 持久化，解决同名题收藏串联；旧标题收藏仅在唯一匹配时迁移。
- 统一列表日期、过滤计数与来源分类口径，并降低日常工作台刷新时的全量卡片扫描开销。
- 增加答案索引更新时间和“刷新索引后重试”入口。
- 修复跨学习集定位提示接力、逐题定位动画串联、窗口恢复越界及重复标记菜单语义。
- MN Utils 长按入口采用公开的 `MNButton.addLongPressGesture` 静态签名。
- 总览、错题列表、时间线和导出页完成去容器化与 React 状态收口，减少无语义卡片嵌套。
- 错题备份增加两级轮换，并限制高频重复写入。

完整影响、兼容性和验证记录见 `docs/changes/2026-09-01-p0-p1-fixes.md`。

验证：TypeScript 检查通过，自动化测试 149/149 通过，生产构建通过。悬浮入口、跨学习集定位、同步提示及正式版首次启动提醒仍需 MarginNote 4 真机回归。

## MNButton 第二入口与原题聚焦兜底

### 真机反馈修正

- 修正拖动手势位移不断累加造成的悬浮按钮乱飞，改为按触点与按钮初始偏移量计算绝对位置。
- 参照 MN Utils 插件商店迷你入口：拖动结束后计算距四边的距离，选择最近边并以 MN Utils 动画吸附；窗口布局变化后保持在所选边缘。
- 点击与拖动直接绑定在同一个 `MNButton`，点击直接使用插件既有的 `toggleWebPanel:` selector。
- UIStatus 选中显式携带当前学习集 ID，不再从可能属于另一窗口的 `focusWindow` 推导；同时兼容原生 `NSArray` 形式的脑图选择列表验证。
- 定位日志新增开关查询来源、原始返回值、最终判定、实际跳转方法、学习集 ID、调用返回值和异常信息。
- URL 选中改为等待脑图真实应用目标（最长 1.2 秒），避免固定等待 80ms 后误判失败。
- 根据真机日志撤回额外的内置 `FocusNote` 命令：该命令会清除 URL 刚建立的选中状态；关闭同步开关时严格遵循 MN Utils 实现，仅通过 UIStatus URL 选中并验证目标。
- 修正跨学习集时 `openURL` 返回 `false` 但 MarginNote 实际已经切换、插件却提前报错的问题；现在把返回值仅作为日志，继续观察真实学习集状态并完成定位。
- 跨学习集待定位目标增加 30 秒自动过期的轻量接力记录，解决学习集切换期间插件控制器重建导致 pending 目标丢失；成功、失败或过期后自动清除。

### 新增
1. **MN Utils 第二入口**：检测到 MN Utils 时，用 `MNButton` 在当前窗口创建可拖动的插件图标入口；点击与原入口一样切换工作台。未安装或未加载 MN Utils 时静默跳过，不影响原入口。
2. **入口防丢失**：拖动后同时约束横向和纵向位置，保证悬浮入口不会完全移出当前窗口。

### 修复
1. **感知“文档与脑图同步选中”开关**：开关开启时继续使用 `focusNoteInMindMapById`；开关关闭时改用 MN Utils 的 `selectNotesInMindmap([noteId], false)`，通过 UIStatus URL 直接选中脑图卡片。
2. **保留验证与重试**：官方聚焦和 URL 选中都继续经过焦点、可见焦点和脑图选择列表三路验证；失败时保留原有重试、跨学习集打开和脱敏日志。
3. **安全降级**：MN Utils 或相关接口不可用、开关状态无法读取时，自动回退 MarginNote 官方聚焦方法。

### 升级提示
安装前已备份升级前工作区。安装后请完全退出并重启 MarginNote 4；第二入口仅在 MN Utils 已安装并加载时显示。
