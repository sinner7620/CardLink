# v2.3.3-beta.97

基线：beta.96。依据排查报告与 AI 错题总结方案做根治性修复。

## 修复

- AI 错题分析默认关闭且关闭期零运行：仪表盘携带 `aiEnabled` 门闸，总开关关闭时 Web 不发送任何 AI 命令、总览 AI 模块只显示"未开启 + 前往配置"；原生侧把 AI 命令拆为配置段（始终可用）与运行时段（开启后可用）。
- 根治错题加载停滞的线程饥饿：AI 分析改用只读内容提取（不同步标签、不写错题库），并从"每 4 条让出 10ms"改为逐题让出，批量读取不再独占 JS 线程。
- 分页续传失败后"正在载入错题 25/N…"横幅不再永久滞留；完结页不再残留上一页续传游标。
- 调试模式开关与插件开关共用即时回显数据流，开启后立即出现在设置页，不再依赖大错题库下的整页重载。
- 答案查找窗口悬浮条控件统一为 40×40 并放大（条高 52），关闭/刷新/候选三控件统一显式字号与圆角；缩放手柄统一 44×44，不再被改写为 30×30。
- OCR 缓存键从 32 位采样弱哈希改为 SHA-256 全量内容指纹（新增 `src/content-fingerprint.ts`），杜绝不同图片错配缓存；旧缓存按可再生成缓存处理。
- MinerU 批量结果按 `data_id` 结构化回对，不再全文扫描 zip 链接、不依赖返回顺序；解析失败按题目计入不可用。
- AI 手动分析增加同科目并发抑制，调度跳过有活动任务的科目，终态任务仅保留最近 10 条。
- 300 条截断在报告中显式披露（coverage `total/analyzed` + limitations 说明），分析集按最近更新确定性截断。
- AI 报告证据引用经存在性校验，幻觉 Q 编号剔除并计入 limitations。
- "手写内容"隐私开关生效：手写绘制仅在显式允许时进入 MinerU 上传集合，默认不上传。
- `aiListStudySets` 过滤非脑图类型学习集；报告过期指纹按科目记忆化，不再每份报告全量重算。

## 验证

- TypeScript 检查通过。
- 自动化测试 200/200 通过（新增 SHA-256 标准测试向量与门控/指纹/并发抑制源码断言）。
- 正式渠道 `.mnaddon` 构建通过，源文件与 iCloud 交付副本 SHA-256 一致：`1b1ab2329c7bf8115c5b174bc8ff257ad56c8ed02a368709511b819219073cbd`。

## 未验证

- 需在 MarginNote 4 真机复测 880 条错题的分页连续性、AI 配置页、调试开关回显与悬浮条新尺寸触控。
- MinerU `file_results`/`data_id` 回显与三家 Provider 真实凭据调用未执行。
- 方案中的三级分析、语义缓存、任务持久化与 Provider 契约层属后续阶段，本轮未实现。
