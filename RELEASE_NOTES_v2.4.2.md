# CardLink v2.4.2

## 更新说明

以下内容以 v2.4.1 正式版（`1a6a37b`）至本版的实际代码差异为准核对。

### AI 错题分析

- 题目输入改为混合模型：有题干文字的题目直接读取原生文字，不上传任何内容；含图片的题目才把去除手写后的整张卡片交给 OCR 识别成文字发送。未开启题目识别时图片题会明确提示，不再静默失败。
- 新增「手写内容」统一开关：卡片内手写与脑图绑定手写合并管理。手写永远不会进入 OCR 截图；开启后，准备阶段把手写单独保存为图片，分析时随对应题目直接发送给分析模型（单次最多 30 张，需模型支持图片输入）。
- 发送行为与设置严格一致：分析前核对题目、手写与发送范围，来源或设置变化后旧 OCR 自动失效、提示重新准备；取消任务、关闭 AI 或修改发送设置会终止进行中的分析并丢弃迟到结果，不再保存过期报告。
- 报告更真实：按完整题目控制请求长度，引用与覆盖数只对应实际发送的内容，缺失的手写图片数量会在报告说明中注明；预检直接显示可用与需准备题数。
- 纯文字错题不再被静默丢弃：未做过 OCR 准备的纯文字题目，此前会被误计入“内容不可用”而整题跳过，现在可直接参与分析（与预检口径一致）。
- AI 配置页全新改版：iOS 风格开关、「AI 服务」单入口（下拉选择分析 AI，题目识别 OCR 配置收纳为独立条目）、发送内容精简，并新增密钥清除入口。

### 卡片预览与错题管理

- 预览缩放后不再贴在左上角：有剩余空间时居中显示，长内容保留顶部起始和滚动。
- 预览文档双击（双轻点）可切换显示脑图绑定手写；摘录图片也会渲染配对的手写叠加，并修复叠加画布与底图错位拉伸的问题。
- 答案卡片中的 MarginNote Markdown 图片（`markdownimg`）可正常显示，并随 Markdown 错题导出。
- 原卡片仅修改标题时，错题列表与详情的标题即时同步（此前列表保持旧标题）。
- 已删除原卡片的错题现在可以正常取消和批量移除（此前因无法清除标签而卡住）。

### 答案匹配

- 答案候选中已被删除的答案卡片自动跳过：一张失效卡不再导致整个答案候选列表加载失败（索引本身仍只在手动刷新时修正）。
- 答案索引快照为空（已建立但 0 张卡）时不再误报「答案索引尚未建立」。
- 手动刷新答案索引时重新探测脑图卡片列表形态：学习集尚未同步完整时建立的错误形态不再被长期缓存（此前可能导致索引漏卡或重复，需重启插件才能恢复）。

### 界面与导出

- 应用报错提示改为顶栏下方的独立浮层，不再挤动页面内容，可手动关闭。
- 「自动」分页采用 60% 卡片宽度的紧凑排版，约 10 道纯题目一页，题目间仅细分隔线；答案可选「文末集中」或「紧随题目」。
- 导出快速预览按 60% 缩放显示 A4 版面，无需横向滚动；修复正文段落的额外留白。
- 顶栏关闭/刷新按钮保持胶囊样式，并缩小到与顶栏选中标签同高（72×32）对齐。

## 数据与兼容性

- 旧「卡片内手写」「脑图绑定手写」「手写原图直传」开关自动迁移为「手写内容」；手写内容默认关闭，升级不会自动扩大外发范围。「图片上传」选项已移除。
- AI 题目快照策略指纹升级：升级后需重新准备一次错题题目；旧混合 OCR 快照可查看，但不能直接用于分析。
- 既有错题、复习记录、答案索引和报告数据保持兼容；MinerU / GLM-OCR 共用有效的 OCR 策略。
- 移除未使用的腾讯云遥测中继目录与文档，不影响插件功能。
- 使用正式插件 ID `marginnote.extension.mn4-answer-matcher`、标题 `CardLink` 和 `stable` 构建渠道。

## 验证

- `pnpm check`：通过。
- `pnpm test`：262/262 通过。
- `pnpm build`：通过。
- 本包为 v2.4.2 同版本替换：并入纯文字错题分析修复与答案匹配治理后重建，替代 2026-09-14 00:09 的首版构建（经作者明确授权）。
- 正式安装包 `dist/CardLink-v2.4.2.mnaddon`：581,349 bytes。
- 已复制至 `E:\iCloudDrive\同步文件夹\CardLink-v2.4.2.mnaddon`；源文件与交付副本 SHA-256 均为 `c53e9e4ad7862cad671839b0c1bfadfec7fcd4e89e5c0f5ad95a06e95aeba007`。
- 包内 `mnaddon.json` 已确认版本 `2.4.2`、正式插件 ID `marginnote.extension.mn4-answer-matcher` 和标题 `CardLink`。

## 未验证限制

- AI 链路（OCR 线上请求、手写图片捕获质量、多模态请求被模型服务接受）仍需在配置有效密钥的 MarginNote 4 真机环境端到端复验；DeepSeek 官方接口不支持图片输入，开启手写内容向其发送会得到服务端错误。
- 预览手势（双击切换手写）、顶栏胶囊与导出 PDF 的真机呈现待真机验收。
