# v2.3.3-beta.57

> 后续结论：本版的延迟调起日志保存器未改善真机体验，且完整 dashboard 仍会阻塞启动；两项已在 beta.58 撤回或替换。

## 性能与界面修复（2026-09-02）

- 运行日志写完临时文件后先结束 Web 桥接请求，再异步调起系统保存面板；导出期间不再锁住整个插件页面。
- 冷启动先绘制插件外壳，再请求完整 dashboard；首次页面加载与面板显示回调不再重复读取同一份数据。
- 错题列表统一为三行：标题与等级、来源、剩余天数/收藏状态/自定义标签；第三行按点号自然分隔，收藏状态明确显示“已收藏”。
- 待复习答案增加专用响应式视口，iframe 与外层显示区宽高保持一致，缩放只改变答案内容而不改变视口。
- 版本迭代为 `2.3.3-beta.57`。

验证：`pnpm check`、162/162 自动化测试、Playwright 920px/500px 视口检查和生产构建通过。冷启动耗时及系统保存面板仍需在 MarginNote 真机复核。

完整记录见 `docs/changes/2026-09-02-beta57-startup-log-list-answer.md`。
