# 功能页：待复习
## 入口
main.jsx `DueReviewList`。
## 功能
今日/已逾期/未来/已结束四态队列、复测评价（不会/不熟/掌握，实时显示下次日期）、完整原题懒加载（默认收起，A+B）、答案查看缩放、历史时间线。从错题详情的剩余天数胶囊进入时，目标题目滚动到队列视口中央并短时高亮。
## 边界
- 详情只为已展开记录请求（并发 4）；手动补入今日队列
## 验收
切页即时（不随到期数线性变慢）；复测后队列与倒计时实时更新；跨页目标可见、居中且有强调反馈。
