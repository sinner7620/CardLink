# v2.3.3-beta.108：迁移弹窗文案调整

日期：2026-09-07。范围：仅调整错题迁移确认弹窗的展示文案与测试断言，不改变迁移逻辑、持久化键或桥接命令。

## 变更目的

- 说明错题等级从原六档按对应关系迁入新三档，明确提示迁移不可逆。
- 补充旧卡片标签识别、纯答案记录处理和标签暂时不可读时的保留策略。

## 已实现行为

- 弹窗标题由“重要提醒”调整为“迁移提示”。
- 主文案改为说明：为简化复习流程和等级划分，接受后将原六档错题按对应关系迁入新三档，且迁移不可逆。
- 说明文案明确错题标签识别规则、无托管标签或纯答案记录的处理方式，并说明标签暂时不可读时会保留记录。
- 按钮文案由“取消并退出”调整为“取消”，接受按钮由“接受并刷新索引”调整为“接受并刷新”。

## 影响文件

- `web/src/main.jsx`
- `tests/plugin-events.test.ts`
- `package.json`
- `RELEASE_NOTES_v2.3.3-beta.108.md`

## 兼容性与数据影响

- 不涉及错题数据结构变更。
- 不涉及迁移键、完成标记或桥接命令变更。
- 迁移逻辑保持不变：接受后仍执行索引整理并写入完成标记，取消仍只关闭面板。

## 验证

- `pnpm check`
- `pnpm test`
- `pnpm build`
- 已将 `dist/mn4-answer-matcher-v2.3.3-beta.108.mnaddon` 复制到 `E:\iCloudDrive\同步文件夹\mn4-answer-matcher-v2.3.3-beta.108.mnaddon`。
- 源文件与复制件 SHA-256 一致：`27D13CF4C0959013145470AFD6D94A8B5104ABBED2056CA42658B9C864F377B0`。

## 未验证限制

- 构建工具仍报告已有的 `.sfIconGlyph:svg` CSS 伪类警告；不影响本次构建成功。
