# 仓库规则、文档导航与验证流程整理

## 目的

消除过期版本基线、重复文档要求和验证范围歧义，让日常开发与安装包交付分别执行适当检查。

## 已实现行为

- 重写仓库 AGENTS.md：按任务记录变更、按影响范围验证、仅交付安装包时迭代版本，保留正式插件身份、Morphicons、完整交付检查与复制哈希要求。
- 新增 docs/workflow.md，记录现有命令的覆盖差异、定向测试示例、发布流程与工作区整理边界。
- 更新文档索引、验收前置、历史边界和设计文档；删除“新增文件必配同名文档”要求，标明历史资料不是当前执行计划。
- README 改为引用版本来源，增加开发流程入口；历史发布说明仍在根目录，避免破坏 CI 的路径约定。

## 影响文件

- AGENTS.md、README.md。
- docs/README.md、docs/workflow.md、docs/acceptance.md、docs/boundaries.md。
- docs/design/interaction.md、docs/design/design-spec.md。
- 本变更记录。

## 兼容与数据影响

仅修改 Markdown。产品仍为 2.4.1，不修改代码、测试、构建脚本、依赖、CI、个人技能或全局配置；不改变安装包和用户数据。原有未提交删除项及未跟踪文件保留，不移动或删除历史发布说明。

## 验证

- 已只读核对 package.json、tsconfig.json、build.mjs、发布 workflow 和测试文件清单；未审查业务实现或断言质量。
- 已检查本轮文档差异与规则一致性；9 份修改/新增文档中的 92 个本地链接均可解析到现有路径。
- `git diff --check` 通过；已有删除项及未跟踪内容与开始时一致（本轮新增的两份 Markdown 除外）。
- 本轮不运行 pnpm check/test/build：没有应用或构建输入变化，不产生新安装包或发布。

## 未验证限制

- 未测量测试耗时，不宣称具体加速比例；已有测试和 CI 全部保留。
- 旧模块文档和历史报告未逐条对照业务代码，仅明确其适用范围。真机行为未验收。
