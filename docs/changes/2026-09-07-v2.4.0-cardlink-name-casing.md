# 2026-09-07 — 插件名称统一为 CardLink（v2.4.0 同版本覆盖）

## 变更目的

将插件正式版与 Beta 版的品牌名称大小写统一为 `CardLink` / `CardLink Beta`。本轮经用户确认覆盖 `2.4.0`，不迭代版本号。

## 实现行为

- 安装清单标题统一为 `CardLink`，Beta 构建标题统一为 `CardLink Beta`。
- 运行时版本信息标题及正式版/Beta 共存提醒同步使用相同大小写。
- README、本版本发布说明与本地源码预览标题同步更新。
- 插件 ID、npm 包名、版本号、更新仓库和所有功能逻辑保持不变。

## 受影响文件

- `build.mjs`
- `src/plugin.ts`
- `README.md`
- `scripts/build-full-ui-preview.mjs`
- `RELEASE_NOTES_v2.4.0.md`
- `docs/changes/2026-09-07-v2.4.0-maintenance-fixes.md`

## 兼容性与数据影响

仅修改显示名称大小写，不涉及任何数据、配置、接口或迁移行为。

## 验证

- 按用户要求未运行 `pnpm check`、`pnpm test` 或 UI 验证。
- 仅执行 `pnpm build` 生成安装包，并覆盖复制到 `E:\iCloudDrive\同步文件夹\mn4-answer-matcher-v2.4.0.mnaddon`。
- 构建原件与交付副本 SHA-256 一致：`7E79B92D73CD167F1A97B17272A19FD8D14892653415F6FDA4BC7AFD505BB753`。

## 未验证限制

按用户要求未额外打开安装清单或运行界面验证。
