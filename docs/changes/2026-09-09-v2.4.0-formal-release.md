# 2026-09-09 — CardLink v2.4.0 正式发布

## 变更目的

将已完成开发与维护验证的 `2.4.0` 版本作为正式版发布，并按用户提供的内容整理正式 Release 说明、重新生成安装包并完成交付校验。

## 实现行为

- 保持 `package.json` 正式版本号 `2.4.0` 与 `mnChannel: "stable"`，继续使用正式插件 ID 和标题。
- 将 `RELEASE_NOTES_v2.4.0.md` 更新为本次正式发布说明，突出 MNUtils 前置依赖与安装正式版后停用 Beta 版两项提醒。
- Release 说明完整列出错题三级体系、复习计划、导出任务、Bridge 分块传输、窗口与工具栏重构等新增、优化和修复内容。
- 基于当前源码重新执行检查、测试和正式构建，并以新构建产物覆盖交付目录中的同名正式版安装包。
- 创建并推送 `v2.4.0` 正式标签，通过仓库发布流程生成 GitHub Release。

## 受影响文件或模块

- `RELEASE_NOTES_v2.4.0.md`
- `docs/changes/2026-09-09-v2.4.0-formal-release.md`
- `dist/CardLink-v2.4.0.mnaddon`（构建产物，不纳入 Git）
- `E:\iCloudDrive\同步文件夹\CardLink-v2.4.0.mnaddon`（交付副本）

## 兼容性与数据影响

- 正式插件 ID、持久化键与正式更新通道保持不变。
- 旧六档错题等级按现有迁移逻辑转换为「不会 / 不熟 / 掌握」三级，保留用户错题及复测数据。
- Beta 版与正式版并存可能造成重复入口或数据干扰，用户需在安装正式版后停用 Beta 版。
- 部分功能依赖用户另行安装 MNUtils；本轮不打包或自动安装该前置插件。

## 验证

- `pnpm check`：通过。
- `pnpm test`：通过，219/219。
- `pnpm build`：通过，生成 `dist/CardLink-v2.4.0.mnaddon`（518,825 字节）。
- 包内 `mnaddon.json` 已核对：`title: "CardLink"`、`version: "2.4.0"`、`addonid: "marginnote.extension.mn4-answer-matcher"`。
- 已覆盖复制至 `E:\iCloudDrive\同步文件夹\CardLink-v2.4.0.mnaddon`（518,825 字节）。
- 构建原件与交付副本 SHA-256 均为 `2863E8468AE8420A21C6B4D52A78E4E35588249FA567932123C16D90CDA2BD75`。
- GitHub Actions 的依赖安装、`pnpm check`、`pnpm test`、`pnpm build`、版本标签核对与 GitHub Release 发布步骤均通过。
- GitHub Release 已发布为非草稿、非预发布的 Latest 正式版：[CardLink v2.4.0](https://github.com/sinner7620/CardLink/releases/tag/v2.4.0)。
- GitHub 云端构建附件为 `CardLink-v2.4.0.mnaddon`（518,770 字节），平台记录 SHA-256 为 `A79640CC98C6901751D66D7FC95F52C845E01710EF2CE3C6839DC6C084074FD6`。云端与本地构建来自同一标签提交；ZIP 元数据及构建平台不同，因此安装包文件级哈希不同。

## 未验证限制

- 尚需在 MarginNote 4 真机环境逐项确认正式安装包的安装、旧数据迁移、MNUtils 联动与 Beta/正式版切换体验。
- 构建工具仍报告既有的 `.sfIconGlyph:svg` CSS 伪类警告；未影响构建完成。
- GitHub Actions 的 Gitee 镜像同步步骤失败：`scripts/sync-gitee-release-notes.mjs` 在调用处引用了未定义的 `apiUrl`。GitHub Release 已正常完成，本轮未修改已发布标签或同版本插件内容以规避版本复用。
