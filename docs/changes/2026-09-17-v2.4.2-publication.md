# v2.4.2 正式发布

日期：2026-09-17。

## 目的与行为

- 按用户提供的“新增 / 优化与修复”正文发布 CardLink `v2.4.2` 正式版。
- 发布附件固定使用用户指定的 `E:\iCloudDrive\同步文件夹\CardLink-v2.4.2.mnaddon`，不以重新压缩的 CI 构建包替换。
- 发布流程在同名 GitHub Release 附件已经存在时保留该附件，并在同步 Gitee 前重新下载这一附件，保证两个平台使用同一个安装包。

## 影响范围

- 正式发布说明：`RELEASE_NOTES_v2.4.2.md`。
- GitHub / Gitee 发布流程：`.github/workflows/release.yml`。
- 本次标签包含组成指定安装包的现有源码、测试、模块文档和预发布记录；未纳入无关的网站目录、临时文件、生成素材与仓库规则改动。

## 兼容性与数据影响

- 安装包内版本为 `2.4.2`，插件 ID 为 `marginnote.extension.mn4-answer-matcher`，标题为 `CardLink`，构建渠道为 `stable`。
- 本次发布操作不新增数据迁移，也不更改用户数据。

## 验证

- `pnpm check`：通过。
- `pnpm test`：272/272 通过。
- `pnpm build`：通过；存在既有 `.sfIconGlyph:svg` CSS 选择器警告，不影响安装包生成。
- 指定安装包：588,900 bytes，SHA-256 `BBD8528B4CDA88C359BDD7F27DE0D2A4C5519ED755E541253C22C6DBB8F1B4F1`。
- 已解包核对 `mnaddon.json` 的版本、插件 ID 和标题。

## 未验证限制

- 本轮未重新执行 MarginNote 4 / iPad 真机验收；各功能的真机验证状态沿用对应变更记录。
