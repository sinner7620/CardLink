# v2.4.2 正式发布

日期：2026-09-13。基于 2.4.1-b10 工作树（含同日顶栏胶囊 72×32 对齐）。

## 范围

v2.4.1 正式版之后的全部用户可见变化打包为正式版 2.4.2，更新说明见 `RELEASE_NOTES_v2.4.2.md`：AI 混合题目输入（文字直读/图片 OCR）、手写内容统一开关与直传分析模型、AI 配置页重做与单入口、发送一致性（取消/指纹/按整题装包）、导出自动分页 60% 卡片宽度紧凑排版与预览缩放、顶栏胶囊 72×32 对齐。

## 验证与交付

- 交付前对最终输入重跑 `pnpm check`、`pnpm test`（257/257）、`pnpm build`。
- 正式安装包 `dist/CardLink-v2.4.2.mnaddon`（581,299 bytes）。
- 已复制至 `E:\iCloudDrive\同步文件夹\CardLink-v2.4.2.mnaddon`；SHA-256（源与复制件一致）：`4e77b8df74b9ffdcf2e62bb201fa205dcbd3be459a41924d14eb84d13d7ac24f`。
- 解包核对 `mnaddon.json`：版本 `2.4.2`、ID `marginnote.extension.mn4-answer-matcher`、标题 `CardLink`、`stable` 渠道。

## 限制

- GitHub/Gitee 远端发布未执行；按仓库规则需用户另行授权发布。
- AI 链路真机端到端（OCR、手写捕获、多模态请求）与导出 PDF 真机打印呈现仍待 MarginNote 验收。
