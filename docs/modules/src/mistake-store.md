# mistake-store.ts — 错题存储
## 职责
错题库持久化与多级恢复。主存储为插件本地存储（v2 键），备份落文档目录真文件。
## 关键导出
- loadMistakeState：主存储 → 旧备份键 → 备份文件 → v1，回退不合并；恢复即写回对齐
- saveMistakeState：主键覆盖写 + 备份文件重写 + 旧迁移源键置空瘦身
## 边界
- 进程内 cachedState：读一次后不再碰存储
- 旧键只读兼容，健康时自动置空
## 验收
删除记录后冷启动不复活；主存储损坏可从备份恢复；保存耗时/字节进运行日志。