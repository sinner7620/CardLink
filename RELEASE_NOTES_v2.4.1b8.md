# CardLink v2.4.1b8

## 新增

- OCR 成功后除单题文本 JSON 外，同时保存实际发送给 OCR 的整张题目卡片 JPG；开启脑图绑定手写时，保存的就是包含该手写的合成整卡图片。
- AI 配置页“缓存与报告”新增 OCR 结果浏览器，可切换不同题目，左右对照发送原题卡片和 OCR Markdown 渲染文本。
- 题目详情显示 OCR 引擎、来源学习集、识别时间，以及是否包含脑图绑定手写和手写项数。

## 存储整理

- 文档目录下的 AI 缓存、题目文本/图片、报告、错题库备份和 Markdown 导出统一进入 `CardLink/`。
- 缓存目录下的答案索引统一进入 `CardLink/indexes/`。
- OCR 解压、运行日志、更新包和 PDF 渲染临时文件统一进入临时目录的 `CardLink/temp/`。
- 首次访问时迁移旧 `MNAnswerMatcher/` 目录、文档根目录错题备份和缓存根目录答案索引；复制确认成功后才删除旧位置。

## 数据结构

- 继续采用“一题一个 JSON”的题目文本快照，以 `recordId` 的 SHA-256 命名。
- 新快照升级为 schema 2，并以相同散列文件名保存 JPG；重新 OCR 同一道题时覆盖该题的文本和图片。
- 按图片内容指纹保存的 OCR API 缓存继续独立存在，用于避免重复请求服务商。

## 验证

- 定向测试：通过，103/103。
- `pnpm check`：通过。
- `pnpm test`：通过，231/231。
- `pnpm build`：通过并生成 `CardLink-v2.4.1b8.mnaddon`；构建输出含一条既有 `.sfIconGlyph:svg` CSS 警告。
- 安装包大小：575,760 bytes；SHA-256：`81DE52AFB6D6423211958030E6DC1A984F43F299865DC6C9A8F32ED607263857`。
- 已复制至 `E:\iCloudDrive\同步文件夹\CardLink-v2.4.1b8.mnaddon`，交付副本哈希一致。

## 未验证限制

- 旧版本只保存 OCR 文本，没有保存原题卡片图片；旧记录可继续查看文本，但需要重新 OCR 才能显示原图对比。
- 当前环境无法在用户 iPad 的 MarginNote 4.4.5 内验证目录迁移和大批量图片浏览的实际表现。
- 构建中的既有 `.sfIconGlyph:svg` CSS 选择器警告未在本轮处理。
