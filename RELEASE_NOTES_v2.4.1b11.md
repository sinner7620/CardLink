# CardLink v2.4.1b11

## 界面修正

- 将插件页关闭/刷新胶囊缩小为 `88×36px`，高度与旁边“错题本”选中胶囊严格一致。
- 查找答案窗口同步改为 `88×36pt`；双键各占 44pt，继续保持无中缝与整体果冻回弹。
- 同步收紧顶栏拖动避让区域，避免胶囊缩小后留下多余空白。

## 兼容与数据影响

- 关闭、刷新、候选答案和左右换边功能不变。
- 不修改用户数据或存储结构。
- 保持正式插件 ID、标题和 `stable` 构建渠道。

## 验证

- `pnpm check`：通过。
- `pnpm test`：233/233 通过。
- Playwright 实测目标页签与控件胶囊高度均为 `36px`。
- `pnpm build`：通过；存在一条与本轮无关的既有 CSS 选择器警告。
- 安装包：`CardLink-v2.4.1b11.mnaddon`，576,322 bytes。
- SHA-256：`61D30F6F8C92EA9113CDE3F5611DB0B4CABC6489D9B42E9CDAC485656BD6EEDD`。
- 已复制至 `E:\iCloudDrive\同步文件夹\CardLink-v2.4.1b11.mnaddon`，交付副本哈希一致。

## 未验证限制

- 查找答案窗口的原生视觉需在用户 iPad 的 MarginNote 4 中最终确认。
