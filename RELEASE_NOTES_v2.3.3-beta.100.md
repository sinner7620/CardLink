# v2.3.3-beta.100

基线：beta.98。AI 配置页滚动修复、摘要行规范重构与宽面板布局修复（本轮曾以 beta.99 构建中间产物，未交付即被本版取代）。

## 修复

- AI 配置页无法下滑：`.aiSettingsPage` 此前不在工作台共享页面滚动清单（`controls.css` 的 `overflow:auto`/`touch-action:pan-y`）与 `shell.css` 的 `main > section` 内边距清单中，页面既不能滚也贴边显示。已加入三处清单，与总览/设置/导出/复习页同规范。
- 配置页整页被压缩成一条窄列：`.aiSettingsPage` 用 `max-width + margin:auto` 居中，作为 flex 子项时 auto 边距会吸收全部剩余空间、使整页按内容收缩（fit-content）。改为 `width:min(920px,100%)`，宽面板下占满 920px 居中，窄面板占满。
- 配置页按方案"默认只显示摘要行；点击后展开编辑"重构：
  - 科目行只显示科目名、学习集数量、定期状态与编辑入口；展开后编辑名称、学习集勾选（限高内滚）、定期与删除。
  - AI 服务行只显示名称、Provider、模型、默认徽标与密钥状态（`已配置 ····尾四位` 掩码）；展开后编辑地址/模型、设为默认、密钥与测试。
  - 题目识别（MinerU）与发送内容同样摘要行＋展开；同一时间只展开一个编辑器。
- 级联层修正：摘要行样式最初写入 `mn-ui-base`，被 priority 层的 `.settingsGroup button` 网格规则整体压制（层顺序优先于特异性），导致行内文字压缩换行。相关覆盖规则已按所有权约定写入 settings.css 的 `mn-ui-priority` 块，并显式取消摘要行的"›"伪元素、把编辑器按钮恢复内联尺寸。
- 学习集复选框修复：编辑器内 `input{width:100%}` 误伤复选框（拉到 107px 宽把文字挤成竖排），收窄为 `.aiEditorField > input`；学习集标签显式保持横向 flex。
- 总览 AI 面板空态留白收窄，贴近"未配置时只显示一句状态和前往配置"。
- 浏览器预览桥补充 AI 命令 mock（设置/学习集/缓存统计），供 UI 预览与自动化验证使用。

## 验证

- TypeScript 检查通过；自动化测试 204/204 通过（含共享滚动清单、摘要行＋掩码尾部断言）。
- **真实浏览器渲染验证**（Playwright + 构建产物，900px 视口）：`.aiSettingsPage` 宽 900 全幅、`overflowY:auto`、scrollHeight 1019 > 760 可滚动；摘要行高 53px 单行显示；展开编辑器内科目名称输入全宽、学习集复选框与文字同行、删除按钮紧凑。
- `pnpm build` 通过；源包与 iCloud 交付副本 SHA-256 一致：`4a0f8ac1cfaab40639e26f5e32e547b00d985babcc37fbc6e425e1d720bf78ee`。

## 未验证

- iPad 真机复测：配置页滚动手感、展开编辑触控、学习集限高内滚、宽窄面板两种布局与总览 AI 面板观感。
