# v2.3.3-beta.72

基线：v2.3.3 正式版；正式项目内版本号迭代为 `2.3.3-beta.72`。自本版起所有开发收敛在正式版项目：**插件保持正式渠道 ID（`marginnote.extension.mn4-answer-matcher`、标题「跨脑图卡片匹配」），版本号按 beta 序列迭代，可在 MarginNote 内从 v2.3.3 原地升级**。本版集中处理 8 项真机/预览反馈，全部从根源修复，不打补丁、不加兜底。

## 渠道与版本号解耦

- `build.mjs` 与运行时插件标识不再从版本号后缀推断渠道：渠道改为 `package.json` 的 `mnChannel`（当前 `stable`）。版本号含 `-beta` 只影响更新检查的预发布通道与遥测渠道标签，不再切换插件 ID/标题。
- 正式/Beta 共存提醒改按渠道判断：正式渠道的 beta 版本号仍会提醒关闭旧独立 Beta。
- 保留给独立 Beta 工作树的能力：`mnChannel: "beta"`。

## 修复内容

- **插件面板恢复自由拖动**：移除拖动逐帧、拖动结束与恢复会话窗口时的位置限制（`clampFrameToStudy` → `clampFrameSize`，只保留尺寸下限与不超过宿主的最大尺寸）。顶部 inset 修复由 `lockWebViewRootScroll`（禁用自动 content inset 调整、强制清零 inset/offset）独立承担，保持不变。
- **标签下拉完整显示**：菜单内标签文字只显示一个字的根因是遗留规则 `.detailHeader button { max-width: 68px }` 误伤菜单选项按钮（该规则同时把等级胶囊压成 68px、与 72px 天数胶囊不齐），遗留规则组已删除；菜单被详情面板 `overflow: hidden` 裁剪、被列表遮挡的问题改为 portal 挂载 body + fixed 锚定触发按钮，窄窗口也完整显示。
- **全部分类与日期筛选弹层不再被预览区遮挡**：两个弹层同样改为 portal + fixed 锚定（侧栏是滚动容器，子元素弹层必被裁剪，z-index 无法穿透）；打开期间任意滚动自动收起，避免弹层与锚点脱节。
- **悬浮条控件统一样式**：等级筛选与批量改级移除边框和底色（保留下拉箭头），同行控件之间新增淡竖线分隔（与详情状态胶囊分隔线同源）。
- **详情区贴齐顶栏、分隔线贯通**：详情头部顶部内边距归零；列表/预览分隔线由上下各内缩 10px 改为贯通全高。
- **窄窗剩余天数胶囊完整**：详情头部不再被压缩（`flex: none`），纵向拼接的等级/剩余天数胶囊不再被预览区盖住下缘；等级与天数胶囊恢复等宽 72px。
- **待复习窄窗图标居中**：定位/答案/复测历史按钮隐藏文字后，图标由 flex gap 保证精确居中。
- **悬停反馈修正**：统一悬停基线不再覆盖带专用状态底色的控件（选中页签、已选列表项、到期芯片、三档复测按钮等）；等级下拉悬停保留箭头；搜索框补齐悬停边框反馈。

## 说明

- 「窗口缩小后标签栏拉出长灰条」未能在现行代码 430–980px 各档窗口复现（对应旧 beta 的灰底页面风格），残留样式入口（遗留详情按钮规则）已清理；如真机复现请提供当前版本截图。
- 弹层 DOM 挂载点移到 `document.body`：按“容器前缀 + 弹层类名”定制的旧选择器已随清理退役，按弹层类名定制的样式不受影响；错题、复习历史、答案绑定与窗口位置数据结构不变。

## 验证

- `pnpm check` 通过；`pnpm test` 178/178 通过；`pnpm build` 通过。
- Playwright 在 980/620/430/480 四档窗口逐项复验：弹层完整显示且覆盖预览区、标签全文可见、批量栏样式统一含淡竖线、胶囊等宽且矮窗口完整、待复习图标居中、悬停状态不被覆盖。
- 包内 `mnaddon.json`：正式插件 ID `marginnote.extension.mn4-answer-matcher`、正式标题「跨脑图卡片匹配」、版本 `2.3.3-beta.72`；可从 v2.3.3 原地覆盖升级。
- 窗口自由拖动手感与拖顶后无顶部空白需在 MarginNote 真机复验。
