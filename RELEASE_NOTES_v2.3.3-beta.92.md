# v2.3.3-beta.92

基线：beta.91（版本号实际以工作树为准递增）。修复真机反馈：查找答案窗口顶部三个控件区域（关闭/刷新/候选长条）无法响应拖动窗口操作，且候选长条点按被拖动区遮挡。

## 根因

拖动手势区（dragArea）此前只覆盖 x≥102 的区域且**创建于各控件之后**：①左侧 102pt（关闭/刷新所在条）与控件之间的空隙完全没有拖动手势，无法从那里拖动窗口；②候选长条（x≈106、y 9–39）落在 dragArea 之下，点按被拖动区吞掉——这正是"候选选择点击无反应"的另一半根因（beta.88 只修了下拉行的 tag 定位）。

## 实现

- 拖动区改为**顶部整条**（`{x:0, y:0, width, height:48}`），并在 `setup` 中**最先加入**（垫在控件底层）；关闭/刷新/候选长条依次叠于其上——控件本身正常点按，控件之间与四周的留白都能拖动窗口。
- 布局同步：`layoutAnswerCardWindowControls` 将拖动区帧更新为全宽 48pt（窗口缩放/侧边切换时保持覆盖）。
- 修复本轮脚本误删造成的 dragArea/手势缺失（窗口创建曾会抛 ReferenceError）。

## 影响文件

`src/answer-card-view.ts`、`tests/plugin-events.test.ts`（新增拖动区全宽/层序断言）、`package.json`（版本号）。

## 兼容性与数据影响

数据结构、存储键、桥接命令不变；正式渠道 ID 原地覆盖升级。

## 验证

- `pnpm check` 通过；`pnpm test` 180/180 通过（含新增拖动区覆盖/层序断言）；`pnpm build` 通过，生成 `dist/mn4-answer-matcher-v2.3.3-beta.92.mnaddon`，已拷贝至 `E:\iCloudDrive\同步文件夹\`（SHA-256 见变更记录）。
- 真机待验证：从顶条任意位置（含三个控件之间/周围）拖动窗口；点击 ×、↻、候选长条仍正常响应。

## 未验证限制（必须真机）

- 控件与拖动手势的命中分配（按钮在上层可点、其余区域可拖）在真机 UIWebView 的实际表现。
- 若从按钮正上方按下并拖动，行为是"触发按钮"而非"拖窗"（符合预期，如需按钮也触发拖动请反馈）。
