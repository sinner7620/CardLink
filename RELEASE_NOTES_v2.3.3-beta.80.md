# v2.3.3-beta.80

基线：beta.79。设置页布局优化：消除宽面板右列空洞、压缩纵向长度、统一页面结构。

## 实现

- **列平衡**：「插件」大类（6 行）从左列移到右列「错题分类说明」下方，联通测试结果与调试功能跟随插件放右列。左列 = 答案匹配 + 错题管理（568px），右列 = 说明 + 插件（741px），此前右列 346px 下方约 600px 的空白消除。
- **去页面大标题**：删除「设置与管理」sectionIntro——顶栏页签已标明位置，且总览/错题本/待复习页均无此类大标题，结构更一致；相关死 CSS 一并清理。
- **说明卡压缩**：三档卡片改为两行布局（等级+描述一行、天数编辑器独立右对齐一行），描述不再与编辑器挤压换行；移除 `min-height: 72px`。
- **行密度**：设置行 `min-height` 52px → 46px。
- **单列顺序**：≤800px 单列时原 DOM 顺序会把说明卡夹在错题管理与插件之间；断点内将两列容器拆为 `display: contents` + flex 纵排，说明卡 `order: 9` 排到末尾——窄面板视觉顺序 = 答案匹配 → 错题管理 → 插件 → 说明。

## 影响文件

`web/src/main.jsx`（设置页 JSX 重排、去标题）、`web/src/ui/settings.css`、`package.json`（版本号）。

## 兼容性与数据影响

数据结构、存储键、桥接命令不变；正式渠道 ID 原地覆盖升级。

## 验证

- `pnpm check` 通过；`pnpm test` 178/178 通过；`pnpm build` 通过，生成 `dist/mn4-answer-matcher-v2.3.3-beta.80.mnaddon`，已拷贝至 `E:\iCloudDrive\同步文件夹\`（SHA-256 见变更记录）。
- Playwright 实测：1180px 宽面板左右列高 568/741（原 940/346）；720px 窄面板视觉顺序为答案匹配 → 错题管理 → 插件 → 说明（截图 `output/playwright/b80-settings-wide.png`、`b80-settings-narrow.png`）。

## 未验证限制

- 真机观感（两列高度差 173px、说明卡两行布局）待复验。
