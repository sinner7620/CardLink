# CardLink v2.4.2-b1

## 更新说明

### 双击查找答案

- 在题目脑图中双击卡片，可直接查找对应答案，并优先调用 MarginNote 4 原生卡片源浮窗定位答案卡片。
- 原生浮窗运行时接口不可用或调用失败时，自动回退到 CardLink 答案窗口，不切换当前主脑图。
- 双击空白区域不会触发查找；单击卡片与原有侧边工具条行为保持不变。

### 缺失答案时生成卡片

- 已绑定答案脑图但没有匹配结果时，仍先由用户确认刷新答案索引；刷新后仍未找到，才询问是否复制当前题目生成待填写的答案卡片。
- 新答案卡片保留题目卡片内容与标题，并在生成后立即刷新索引、尝试在原生浮窗定位。
- 答案脑图已有唯一的同层级分支时直接复用，**不会再次复制新分支**；分支重名或无法唯一判断时不猜测、不新建分支，而是挂到绑定的答案根节点。
- 同一题目的并发生成请求会被去重，避免重复操作生成两张卡片。

## 数据与兼容性

- 使用正式插件 ID `marginnote.extension.mn4-answer-matcher`、标题 `CardLink` 与 `stable` 构建渠道，作为现有 CardLink 的预发布升级包。
- `focusNoteInFloatMindMapById` 属于真实存在但未进入公开 npm 类型声明的运行时接口；插件在每次调用前进行功能探测。
- 只有用户明确确认“生成答案卡片”后才写入答案脑图；不会自动创建缺失的章节分支。

## 验证与限制

- `pnpm check`：通过。
- `pnpm test`：268/268 通过。
- `pnpm build`：通过；构建过程中出现既有 `.sfIconGlyph:svg` CSS 选择器警告，不影响产物生成。
- 安装包 `dist/CardLink-v2.4.2-b1.mnaddon`：583,822 bytes。
- 已复制到 `E:\iCloudDrive\同步文件夹\CardLink-v2.4.2-b1.mnaddon`；源文件与交付副本 SHA-256 均为 `ef57782af923ec27d586089a73fd16d1759fcfbe6479a4f468b72db42e12de7e`。
- 包内 `mnaddon.json` 已确认版本 `2.4.2-b1`、插件 ID `marginnote.extension.mn4-answer-matcher` 和标题 `CardLink`。
- 原生浮窗在已打开/未打开状态下的视觉定位、跨学习集卡片复制与父子关系写入仍需 MarginNote 4 真机验收。
