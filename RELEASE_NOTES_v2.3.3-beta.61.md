# v2.3.3-beta.61

## 复测历史蛇形时间轴（2026-09-02）

- 取消时间轴横向滚动、pointer capture、横向拖动和方向键滚动。
- 根据时间轴容器的实时宽度与复习节点间隔自动分行。
- 第一行从左到右，第二行从右到左，后续依次交替，保持复习时间的连续阅读顺序。
- 相邻行在右侧或左侧尽头通过圆滑 U 形弯道连接。
- 最后一段路径增加方向箭头，明确时间轴结束方向。
- 时间轴高度按行数自动增长，所有节点直接显示，不产生局部或页面级横向滚动。
- 版本迭代为 `2.3.3-beta.61`。

验证：`pnpm check`、158/158 Node 自动化测试、3/3 Playwright 测试和生产构建通过。500px 预览使用 12 个节点验证了 4 行蛇形排列、交替方向、末端箭头和零横向溢出。

完整记录见 `docs/changes/2026-09-02-beta61-serpentine-review-timeline.md`。
