# CardLink v2.4.1b3

## 修复

- 修复 AI 配置页执行错题准备时提示“题目卡片渲染组件未加载”的问题；渲染器现在直接编入主面板应用包，不再依赖仅供 PDF 导出窗口使用的全局脚本。
- “错题题目准备”的学习集下拉框现在只显示实际包含错题的学习集，并显示各学习集的错题数量。
- 当原生侧再次校验发现所选学习集没有错题时，直接提示“无需 OCR”并拒绝创建任务，保证不会发起 OCR 请求。
- 没有任何含错题学习集时，页面显示明确空状态且开始按钮不可用。

## 兼容性

- 保持正式插件 ID、标题、稳定渠道及既有错题/OCR 文本数据格式不变。
- 本轮无需迁移或清理此前已经准备的题目文本。

## 验证

- `pnpm check`：通过。
- `pnpm test`：通过，223/223。
- `pnpm build`：通过。
- 已核对生产 `app.js`：包含内编的 `html2canvas`，旧的“渲染组件未加载”运行时分支不存在。
- 包内清单已核对：正式插件 ID `marginnote.extension.mn4-answer-matcher`、标题 `CardLink`、版本 `2.4.1b3`。
- 构建包与交付副本均为 570,473 字节，SHA-256 一致：`7B9823EAE01BE6AD62162E7055FC4795B0955DD92F875E65578A23894F4D2269`。

## 未验证限制

- 仍需在 MarginNote 4 真机上使用有效 MinerU Token 验证完整上传与识别过程。
